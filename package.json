{
  "name": "server-tsf",
  "version": "1.0.0",
  "description": "backend server for trinational scientifique foundation",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start_prod": "export NODE_ENV=production & nodemon server.js",
    "start_dev": "export NODE_ENV=development& nodemon server.js"
  },
  "author": "ouss-err",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.1",
    "morgan": "^1.10.0",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
